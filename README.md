# mybclochain

This is mybclochain.

```sh
# init project
$ cargo new mybclochain
$ cd mybclochain
$ cargo build


# add lib
$ cargo new core --lib
# add utils
$ cargo new utils --lib

# run
$ cargo run

```